---
date: 2026-09-04T09:19:29.757Z
branch: main
relatedPlan: 2026-09-04-feature-day-8-rag-rag-.md
---

# Session Summary

完成 Day 8「RAG 核心流程」，实现 rag/ 包（10 个任务全部通过）。包含：多路检索（VectorRetriever 复用 ChromaStore、KeywordRetriever 手写 BM25+bigram、HybridRetriever RRF 融合）、重排序（ReRanker 抽象 + CrossEncoderReRanker 懒加载）、PromptBuilder（带 [n] 引用 + 映射表）、RAGChain（端到端编排）。复用 vector_store.ChromaStore 与 llm_client.BaseLLMClient，形成「检索→重排→构建→生成→引用」完整链路。质量门（@testing/@security/@audit/@docs-writer）通过：修复 1 个 security HIGH（间接提示注入缓解）+ CrossEncoder predict 异常包装 + BM25 k1/b 参数校验 + k 参数校验。BM25/RRF 算法经 @audit 逐项核验完全正确。新增 32 个单元测试（离线 fake）+ 3 个 e2e 测试（真实内存 chromadb + mock LLM），全量回归 377 passed。产出 1 份特征文档 + 1 份 ADR 决策文档。

## Key Decisions

- 关键词检索手写 BM25 + 字符 bigram，不引入 rank_bm25/jieba（学习原理 + 轻依赖）
- 多路融合用 RRF（Reciprocal Rank Fusion），量纲无关、多路认可加分
- CrossEncoder 懒加载（模型需联网下载），测试用确定性 Fake
- 生成层复用 llm_client.BaseLLMClient，端到端测试用 mock LLM
- rag 不复用 llm_engine，避免职责耦合
- 修复间接提示注入：PromptBuilder 用 <chunk> 标签隔离 + 数据≠指令声明 + 控制字符清洗

## Files Changed

- `rag/（10 个 .py 文件）`
- `tests/test_rag_retrievers.py`
- `tests/test_rag_rerankers.py`
- `tests/test_rag_prompt.py`
- `tests/test_rag_chain.py`
- `tests/test_rag_e2e.py`
- `docs/features/2026-09-04-rag-package-rag.md`
- `docs/decisions/2026-09-04-rag-bm25-rrf-.md`

## Related Plan

See: `.cortex/plans/2026-09-04-feature-day-8-rag-rag-.md`
